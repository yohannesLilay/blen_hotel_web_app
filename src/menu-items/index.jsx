import dashboard from "./dashboard";
import security from "./security";
import configurations from "./configurations";

const menuItems = {
  items: [dashboard, security, configurations],
};

export default menuItems;
